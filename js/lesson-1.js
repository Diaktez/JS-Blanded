// Задание 1:
// Предложите пользователю ввести число с помощью prompt().
// Сохраните введенное значение в переменную number.
// Проверьте, равна ли переменная number числу 10 :
//     Если да - выведите сообщение 'Верно' в alert().
//     Если нет - выведите 'Неверно' .

// Подсказка: prompt всегда возвращает строку (String), поэтому перед проверкой
// превратите значение в число с помощью Number().
// function giveMeNumber() {
//   let hole = prompt("input number");
//   let bigHole = Number(hole);
//   if (bigHole === 10) {
//     alert("Верно");
//   } else {
//     alert("Неверно");
//   }
// }
// giveMeNumber();
//_______________________________________________
// Задание 2:

// В переменной min лежит случайное число от 0 до 59 .
// Определите, в какую четверть часа попадает
// это число (в первое, второе, третье или четвертое).
// Выведите в alert соответствующее сообщение, например:
//  "10 входит в первую четверть"

// const min = Math.floor(Math.random() * ( 59 - 0 ) + 0 );
// function giveMeNumber() {
//   const min = Math.floor(Math.random() * (59 - 0) + 0); // можно юзать как шаблон, мол сам вібирай числа от 0 до 59
//   const fixMin = Number(min);
//   if (fixMin <= 15) {
//     alert(`${fixMin} ето первая четверть`);
//   } else if (fixMin <= 30) {
//     alert(`${fixMin} ето вторая четверть`);
//   } else if (fixMin <= 45) {
//     alert(`${fixMin} ето третья четверть`);
//   } else if (fixMin <= 59) {
//     alert(`${fixMin} ето четверта четверть`);
//   } else {
//     alert(" в часе так мало времени ");
//   }
// }
// giveMeNumber();
//_______________________________________________________________
// Задание 3:

// Переменная num может принимать 4 значения: '1' , '2' , '3' или '4'
// (спрашивай это значение у пользователя через prompt).
// Если она имеет значение '1', то в переменную result запишите 'зима',
// если имеет значение '2' – 'весна' и так далее.
// Решите задачу через switch-case.
// Не забудьте о дефолтном значении, в случае, если пользователь
// введет в prompt что-то другое. В таком случае result должен приобретать значение:
// "Извините, но вы должны ввести значение от 1 до 4 включительно".
// Значение переменной result выведите в консоль.
// let season = prompt(" какой сейчас период года ?");
// switch (season) {
//   case "1": {
//     console.log(` вібор ${season} зима`);
//     break;
//   }
//   case "2": {
//     console.log(`вібор ${season} весна`);
//     break;
//   }
//   case "3": {
//     console.log(` вібор ${season} лето`);
//     break;
//   }
//   case "4": {
//     console.log(`вібор ${season} осень`);
//     break;
//   }
//   default:
//     console.log(
//       " Извините, но вы должны ввести значение от 1 до 4 включительно");
// }
//_______________________________________________________________________________________
// Получайте от пользователя число (количество минут) через prompt
// и выводите в консоль строку в формате часов и минут.
// Пример: пользователь вводит в prompt '70' -> в консоли отобразится '01:10' .
// Полезная ссылка для отображения часов и минут в нужном формате ( '01' вместо '1' ):
// https://developer.mozilla.org / en - US/ docs / Web/ JavaScript /Reference/ Global_Objects /String/ padStart #examples
// function minToHourse() {
//   let input;
//   while (true) {
//     input = prompt("Введите количество минут:");
//     // Если нажата "Отмена" (input === null), то выход из цикла
//     if (input === null) {
//       console.log("Вы отменили ввод.");
//       break; // Прерывание цикла, если пользователь нажал Отмена
//     }
//     // Проверяем, что введено только число
//     if (/^\d+$/.test(input)) {
//       let hours = Math.floor(input / 60);
//       let minutes = input % 60;
//       console.log(
//         `Часы: ${String(hours).padStart(2, "0")}, Минуты: ${String(
//           minutes
//         ).padStart(2, "0")}`
//       );
//       break; // Выйти из цикла, если введено корректное число
//     } else {
//       alert("Пожалуйста, введите число.");
//     }
//   }
// }
// minToHourse();
//------------------------
// Задание 5:
// Напишите код, который будет запрашивать у пользователя логин с помощью prompt и выводит результат в консоль браузера
// После этого добавьте проверку введенного пользователем значения:
// Если посетитель вводит "Админ" ,
// то prompt запрашивает пароль (не забудьте сохранить его в переменную для последующей проверки).
// Если ничего не ввели или нажали Cancel,
// Вывести в alert строку "Отменено"
// В противном случае вывести в alert строку "Я вас не знаю"
// Пароль проверять так:
// Если введен пароль "Я главный" ,
// то вывести в alert строку "Добрый день!" ,
// в противном случае вывести в alert строку "Неверный пароль!"
//test
// function autorisation() {
//   let admin = prompt("Введите login");

//   if (admin === "" || admin === null) {
//     alert("Отменено");
//     return;
//   }
//   if (admin === "Админ") {
//     const password = prompt("Введите пароль");
//     if (password === "" || password === null) {
//       alert("Отменено");
//     } else if (password === "Я главный") {
//       alert("Добрый день!");
//     } else {
//       alert("Неверный пароль!");
//     }
//   } else {
//     alert("Я вас не знаю");
//   }
// }
// autorisation();
//-------------------------------------------------------------------
//// Используйте цикл while , чтобы вывести в console все числа от 0 до 20 включительно.
// function Numbers() {
//   let hole = 0;
//   while (hole <= 20) {
//     console.log(`число: ${hole}`);
//     hole++;
//   }
// }
// Numbers();
//------------------------------------------------
//Задание 7:
// Напишите функцию getNumbers( min , max ), принимающую 2 параметра -
// минимальное и максимальное число соответственно.
// Напишите цикл внутри функции, который выводит в консоль
// все числа от max до min по убыванию.
// Кроме того, подсчитайте сумму всех четных чисел в этом цикле
// и верните ее из функции.
//--------
// function getNumbers(min, max) {
//   let hole = 0;
//   for (let i = max; i >= min; i--) {
//     console.log(i); // выводим каждое число в консоль
//     if (i % 2 === 0) {
//       hole += i; // суммируем только чётные
//     }
//   }
//   return hole;
// }
// console.log(getNumbers(10, 20));

// Задание 8:

// Напишите функцию min(a, b), которая принимает 2 числа и возвращает меньшее из них.
// Нужно добавить проверку, что функция получает именно числа, в противном случае
// возвращать из функции строку - 'Not a number!' .
// function min(a, b) {
//   if (/^\d+$/.test(a) && /^\d+$/.test(b)) {
//     if (a > b) {
//       return b;
//     } else if (b > a) {
//       return a;
//     } else {
//       return " a = b - change ";
//     }
//   } else {
//     return ` Not a number!`;
//   }
// }
// console.log(min(10, 5));
// console.log(min(5, 10));
// console.log(min("ewe", 10));
// console.log(min(0, 0));
//_________________________________________________
//Задание 9:
// Напишите функцию isAdult(age), которая принимает число - возраст пользователя и возвращает true ,
// если параметр age больше или равен 18 .
// В противном случае она спрашивает подтверждение через confirm
// и возвращает его результат ( true / false ).
// function isAdult(age) {
//   const numericAge = Number(age);
//   if (!isNaN(numericAge)) {
//     if (numericAge >= 18) {
//       return true;
//     } else {
//       return confirm("Вам меньше 18. Продолжить?");
//     }
//   } else {
//     return "Возраст только числами";
//   }
// }
// //function isAdult(age) {
//   age = Number(age);
//   if (isNaN(age)) return "Возраст только числами";
//   return age >= 18 || confirm("Вам меньше 18. Продолжить?");
// }
// console.log(isAdult(18)); // true
// console.log(isAdult(16)); // confirm: true / false
// console.log(isAdult("sda")); // "Возраст только числами"
// console.log(isAdult("20")); // true — тоже работает!

//______________________________________________
// Задание 10:
// // Напишіть функцію fizzBuzz(num), яка приймає число і перевіряє кожне число від 1 до num:
// Якщо число ділитися  без остачі на 3 - виводить в консоль 'fizz',
// якщо ділиться  без остачі на 5 - виводить в консоль 'buzz',
// якщо ділиться  без остачі і на 3, і на 5 - виводить в консоль 'fizzbuzz'.

// function fizzBuzz(num) {
//   num = Number(num);
//   if (isNaN(num)) return "Только числа";

//   for (let i = 1; i <= num; i++) {
//     if (i % 3 === 0 && i % 5 === 0) {
//       console.log(`${i} - fizzbuzz`);
//     } else if (i % 3 === 0) {
//       console.log(`${i} - fizz`);
//     } else if (i % 5 === 0) {
//       console.log(`${i} - buzz`);
//     } else {
//       console.log(`${i}`);
//     }
//   }

//   return "готово!";
// }

// console.log(fizzBuzz(20));
